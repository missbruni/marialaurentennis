import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        'tennis-green': 'var(--tennis-green)'
      }
    }
  }
};

export default config;
